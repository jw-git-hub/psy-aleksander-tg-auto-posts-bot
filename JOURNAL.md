# JOURNAL

2026-09-17: Telegram — второй формат поста `rich` («Статья», `sendRichMessage`, Bot API 10.2+) рядом с `classic` (sendPhoto + sendMessage). Переключатель `telegram_post_format` — только в config.json, без env (иначе .env молча перекрыл бы откат), по умолчанию classic. Rich собирается в режиме `blocks` (publishers/telegram_rich.py): блок-картинка `attach://cover` + абзац на каждую непустую строку. Режим `html` не используем: в доках противоречие «Media blocks support only HTTP and HTTPS URLs» против `tg://photo?id=`, перенос строки внутри абзаца не описан. Явный отказ Telegram (`ok: false` при HTTP 4xx, кроме 429) → classic; сеть/5xx/не-JSON/ответ без явного отказа → исход неясен, classic НЕ шлём (дубль), отказ после такой попытки — тоже провал; `ok: true` с кривым `result` считается отправленным. Без картинки, пустой текст или >500 блоков → сразу classic. В posted-topics.json новое поле `telegram_post_format` (rich правится editMessageText с `rich_message`, не `text`); у старых записей поля нет — это classic. Появились автотесты: tests/ (pytest, поддельный Bot API), `./venv/bin/python3 -m pytest -q`. `preview_telegram_format.py` шлёт образцы в `telegram_test_chat_id` (сверка с основным каналом по строке и через getChat). До прогона в тестовом канале не подтверждено: принимает ли Telegram картинку через `attach://` в rich и нужен ли боту Premium.

2026-09-17: владелец канала вручную исправил в Telegram 5 испорченных постов с утечками техинформации (01.08 10:22 «canthal tilt»/«jawline», 14.08 10:06 «territoria», 07.09 10:25 «Готовый пост от субагента:», 11.09 18:12 «Готовый пост:»+«---», 13.09 18:49 «ЗАГОЛОВОК:»+«head») — отмечено в TODO.md. Пункт про 04.09 (вступление про системный промпт) не подтверждён, оставлен открытым.

2026-09-17: TelegramPublisher._send_photo теперь возвращает (ok, message_id) вместо bool; PublishResult получил photo_post_id. post_bot.py при успешной публикации в Telegram сохраняет в posted-topics.json telegram_chat_id, telegram_message_id, telegram_photo_message_id — нужно для правки испорченных постов через editMessageText без ручного поиска в приложении. Только для новых записей, старые не бэкфилятся (данных нет).

2026-09-17: добавлен доп. семантический барьер против утечек ИИ-текста поверх regex-фильтров из eee4a7d. Проблема: `_META_TECH_PATTERNS` — фиксированный список из 23 regex, новая формулировка утечки (не совпадающая ни с одним паттерном) проходит незамеченной — систематическая причина повторяющихся инцидентов. Фикс: `_check_semantic_leak(text)` (post_bot.py) — вызывает Claude Haiku с финальным текстом поста, просит оценить по смыслу наличие (a) следов ИИ/промпта/субагента, (b) меток-остатков черновика («Готовый пост:», разделители), (c) забытых латинских фрагментов (бренды из `_LATIN_WHITELIST` не считаются нарушением) — ответ строго JSON `{"has_leak", "reason"}`, парсинг через новый `_extract_json_object` (по аналогии с `_extract_json_array`). Fail-open при таймауте/ошибке/непарсящемся ответе (`has_leak=False` + `logging.warning`) — намеренно, т.к. regex уже жёстко блокируют публикацию, эта проверка дополнительная. Встроена в конец `_validate_rewritten_post()` (после всех regex-проверок), при leak — `is_valid=False`, `reason="semantic_leak: ..."`, попадает в существующий путь `continue` в цикле тем. Таймаут 90с (`_SEMANTIC_LEAK_TIMEOUT`, тот же порядок что у L4_SEMANTIC_TIMEOUT). Проверено реальными вызовами claude CLI на 3 примерах: «Готовый пост от субагента:» + «canthal tilt» → поймано, «territoria»/«head» → поймано, чистый текст (с «Instagram») → не поймано (false positive нет).

2026-09-15: утечка техинформации в опубликованные посты («ЗАГОЛОВОК: …», «head» посреди русского текста, «Использую сформулированные в системном промпте требования…», «Готовый пост от субагента:», «Готовый пост:» + «---»). Причины: (1) в REWRITE_PROMPT_TEMPLATE структура была описана буквальной меткой «ЗАГОЛОВОК БОЛЬШИМИ БУКВАМИ», и модель её копировала; (2) `claude -p` запускался из каталога проекта и подхватывал ~/CLAUDE.md владельца (инструкции про субагентов), CLAUDE.md проекта, плагины и хуки. Фикс: все 4 вызова claude идут через `_run_claude()`/`_claude_cli_args()` с `--setting-sources "" --strict-mcp-config --tools "" --disable-slash-commands --no-session-persistence --system-prompt <нейтральный>` и cwd=tempdir (вне домашнего каталога пользователя). `--bare` не использовать: требует ANTHROPIC_API_KEY, а здесь OAuth-подписка. Промпт переписан без слов-меток, с запретом латиницы и мета-комментариев. clean_ai_artifacts срезает метки структуры (однозначные — всегда; совет/вывод/начало/тема — только с «:» и если это заголовок или первая строка) и строки-вступления/разделители (`_LEAK_INTRO_PATTERNS`, `_LEAK_SEPARATOR_PATTERN`). `_validate_rewritten_post` дополнительно отклоняет: оставшиеся метки, мета-лексику (промпт/claude/субагент/нейросеть и т.п.), латиницу вне `_LATIN_WHITELIST`, вступления и любой текст после строки хэштегов. Проверено на text_excerpt 133 постов: срабатывания только на 6 реальных утечках. Таймаут generate_topic_keywords 60→90 с. Уже опубликованные испорченные посты правятся вручную (список в TODO.md), message_id бот не хранит.

2026-07-29 (публикация): проект выложен в публичный репозиторий https://github.com/jw-git-hub/psy-aleksander-tg-auto-posts-bot (портфолио для заказчиков). Репозиторий ПУБЛИЧНЫЙ — любая новая правка не должна вносить секреты. В .gitignore: .env, config.json, posted-topics.json, posted-topics.lock, logs/, venv/, .claude/. Публикуются осознанно: sources.json, CLAUDE.md, JOURNAL.md. Git-идентичность локальная, через GitHub noreply (реальный email в историю не попадает). Уборка: удалены 10 бэкапов (post_bot.py.backup*, .pre-fix-*, sources.json.backup*, posted-topics.json.safety-*) — роль бэкапа теперь у git-истории; logs/ сокращён с 14.9 МБ до 180 КБ. Ротация ужесточена: post.log 5→2 МБ, backupCount 3→2; cron.log 5→2 МБ. Убрано дублирование логов: post_bot.py пишет и в post.log, и в stdout, а run_with_retry.sh складывал весь stdout в cron.log — теперь полный вывод попытки идёт в cron.log только при провале, при успехе только строка с маркером. run_with_retry.sh: хардкод PROJECT_DIR заменён на самоопределение через BASH_SOURCE (портируемость + не светит username), добавлен mkdir -p logs. CLAUDE.md: «50 источников» → 31 (реальное число в sources.json), дополнены зависимости и описание L3/L4.

2026-07-29: аудит + починка post_bot.py (бэкапы: posted-topics.json.safety-20260729-111615, post_bot.py.pre-fix-20260729). L4 (claude -p дедуп) вынесен из цикла кандидатов — раньше вызывался на каждого кандидата (222 таймаута в логах, прогоны 20+ мин), теперь 1 вызов на уже выбранную статью: таймаут 90с, последние 40 заголовков в промпте, fail-open. Константы L4_MAX_CALLS_PER_RUN=6, L4_MAX_REJECTS_PER_TOPIC=3. Ловушка: при отказе кандидата (L4-дубль/провал рерайта/провал валидации) статью ОБЯЗАТЕЛЬНО убирать из пула через `_drop_from_pool()` — иначе следующая тема берёт ту же статью и детерминированно повторяет отказ, прогон сжигает попытки и ничего не публикует. L3: Jaccard → containment (пересечение/min длины), порог L3_CONTAINMENT_THRESHOLD=0.6, отпечаток — полный sorted() без обрезки до 50 (обрезка от set была недетерминированной из-за PYTHONHASHSEED). L2: пороги 0.55 (strong) / 0.35 (при совпадении домена) + >=5 значимых токенов и >=4 общих — старый единый порог 0.35 душил публикации (14 постов на 55 прогонов). Глобальный неблокирующий flock на posted-topics.lock первым действием main() (второй экземпляр — exit 0); save_posted атомарно через os.replace + бэкап .bak; load_posted при битом JSON теперь critical+exit(1) (раньше [] и первая публикация затирала историю). Добавлен `_validate_rewritten_post()` перед публикацией (длина 200..4096, запрет http/markdown-ссылок, обязательные хештеги) — проверено на 138 реальных постах канала, проходят 133, отказы — сервисные сообщения TG и 3 первых поста без хештегов. `_safe_request()` — ручные редиректы с валидацией каждого хопа через `_is_safe_url` (макс 3): download_image, _safe_download_html, _fetch_full_article_body. Контент источников обёрнут в `<untrusted_article_content>` в промптах (prompt injection — результат публикуется без ревью). publishers/log_safety.py — редакция секретов (TG-токен, FB/IG access_token) на root-логгере и хендлерах (requests пишет URL с токеном в текст сетевых исключений). run_with_retry.sh матчит успех по маркеру `INFO Опубликовано: ` (exit code не отличал «опубликовано» от «ничего не вышло») — этот текст в post_bot.py не менять; + ротация cron.log на 2 МБ. RSS теперь через requests timeout=(10,20), парсинг из контента (feedparser.parse(url) не имел таймаута, вешал процесс навсегда). clean_ai_artifacts: паттерны "я не могу/i cannot" — только по первым 2 строкам (re.match), не режут середину постов. Удалён мёртвый дубль TG-клиента (send_photo/send_message/sanitize_for_logging) и get_next_unposted_topic — публикация реально идёт через publishers/telegram.py. Права: .env/config.json — 600, logs/ — 700, логи — 600 (mkdir(mode=) не действует на существующий каталог, нужен явный os.chmod). Осознанно не исправлено: fetch_articles_rss без _is_safe_url (источник — доверенный sources.json); fallback-flock в save_posted недостижим, но потенциально блокирующий; ротация cron.log через mv оставляет cron писать в старый inode; узкое окно двойной публикации при убийстве процесса между отправкой в Telegram и записью posted-topics.json.

2026-07-29 (правка): L2 откачен к исходному единому порогу 0.35 (`L2_JACCARD_THRESHOLD`) по решению владельца — схема 0.55/0.35+домен+мин.токены из правки выше признана ненужным ослаблением (посты и так выходили штатно), убрана вместе с `_same_domain()`. Остальные пункты аудита за 29.07 (L3 containment, вынос L4 из цикла, `_drop_from_pool`, flock, атомарная запись, RSS-таймауты, `_safe_request`/SSRF, `_validate_rewritten_post`, обёртка untrusted-контента, редакция секретов в логах) не тронуты.

2026-05-06: фикс 56→80 + циклическая ротация подтверждён end-to-end через DRY_RUN. Лог: `Цикл 1, неопубликованных в этом цикле: 24 из 80`, кандидаты — все 5 тем из блока 8 (цифровая эпоха), выбрана «Соцсети и ревность», статья «Positive Social Media: How to Use It for Connection» (Haiku score выбрал из 50 после слабого keyword-prefilter), рерайт 1612 симв., симулирована запись с cycle=1/text_hash/text_excerpt. Стат: 89 статей, 12 dups, 14 too_short, 0 text_collisions. Cron 03:00 и 11:00 UTC активен. Прошлые cron-запуски 2026-05-03..05 показывали «56 из 56» т.к. правка кода (mtime 2026-05-05 12:52) легла после крон-окна 11:00.

2026-05-05: расширение тематического плана 56→80 тем (добавлены блоки 8-10: цифровая эпоха, сексуальность/интимность, деньги/быт). Циклическая ротация: compute_current_cycle() определяет цикл по posted-topics.json; get_unposted_topics(posted, cycle) — новая сигнатура. В cycle>=2 передаются excerpts прошлых постов в prompts рерайта. L4 text-level dedup через MinHash (blake2s, 64 хэш-функции, 4-word shingles, порог 0.30) — защита от повтора рерайтнутого текста, работает независимо от L1-L3. posted-topics.json получает новые поля: cycle, text_hash, text_minhash, text_excerpt; старые записи мигрируются in-memory (cycle=1). DRY_RUN=1 — симулирует публикацию без HTTP в Telegram/FB/IG.

2026-04-19: пайплайн перевёрнут article-first → topic-first. Баг: пост «Роль границ в семье» вышел с картинкой-анекдотами (AdMe), т.к. `match_topic` натягивал случайную статью на тему при слабом семантическом пересечении. Теперь: тема выбирается первой (по плану, исключая опубликованные), собирается пул статей из всех источников, Haiku скорит релевантность 0–10. Константы: `MAX_TOPIC_ATTEMPTS=5`, `RELEVANCE_THRESHOLD=7`, `MAX_POOL_SIZE=50`. Fallback — перебор до 5 тем, иначе skip без ошибки. `match_topic` удалён. Добавлен кэш L4-дубль-проверок на запуск. `sources.json` почищен с 50 до 36 (удалены lifestyle/развлекательные: AdMe, Cosmo, Elle, Лайфхакер, The Village и пр.). Бэкапы: `sources.json.backup`, `post_bot.py.backup`, `post_bot.py.backup2`.

2026-04-18: добавлены FB/IG публикаторы (publishers/), Graph API v21.0, graceful fallback если токенов нет.
